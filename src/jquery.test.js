/*jslint browser: true */
/*global jQuery */
// Shell for the plugin code
(function ($) {
    "use strict";

    // Note: this incomplete format for a jQuery plugin would work on the entire selection *at once*, which is not desirable.
    $.fn.test = function () {
        // Plugin code
    }

}(jQuery));